source 'https://rubygems.org'

gem 'roo'
gem 'iconv'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.1'
# Use PostgreSQL as the database for Active Record
gem 'pg'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# See https://github.com/rails/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# To manage login and signup flow
gem 'devise'

# To manage access to views
gem 'cancancan'

# To generate forms to every model
gem 'simple_form'

# To manage constantas in the model an application
gem 'lists_constant'

# To manage grid in the views
gem 'foundation-rails'

# To manage user experience making unobtrusive javascript
gem 'jquery-ui-rails'

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use Unicorn as the app server
# gem 'unicorn'

# Use to manage nested associations
gem 'cocoon', '~> 1.2.6'

# Use to paginate objects in the views
gem 'kaminari', '~> 0.16.3'

group :development, :test do

  gem 'rename'

  # Erd, generator of pdf model about query models in rails
  gem 'rails-erd'

  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'

  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'

  # To have a better frame view to debugg errors
  gem 'better_errors'
end

group :production do
  gem 'rails_12factor'
end
